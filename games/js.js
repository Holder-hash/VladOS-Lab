var one_call = false;
 
function onetime(){
	if (one_call == false){
		alert('Функция «func_one» выполнена');
	}
	one_call = true;
}

function myFunc(){
event.target.src="../anon.png"
document.getElementsByClassName('V')[0].style="cursor: default";
}
